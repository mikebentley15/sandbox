#include <mpi.h>

#include <stdio.h>

int main(int argCount, char *argList[]) {
  printf("hello\n");
  //MPI_Init(&argCount, &argList);
  //MPI_Finalize();
  //int rank;
  //MPI_Comm_rank(MPI_COMM_WORLD, &rank);
  //printf("My rank: %d\n", rank);
  return 0;
}
