# Controll Pipeline

This is a fake Qt Core application for reading robot commands from a
controller, reading sensor data (perhaps a file, perhaps a stream, or a serial
port, but here as a fake source), registering the sensor data, and filtering it
(like a running average filter).
