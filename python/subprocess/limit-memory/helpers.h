#ifndef HELPERS_H
#define HELPERS_H

// allocates a char* with the given number of megabytes
char* allocate(int mb_count);

// fills the buffer with zeros
void fill(char* buffer, int mb_count);

#endif // HELPERS_H
